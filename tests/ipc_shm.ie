# POSIX shared memory IPC tokens.
import ipc

tok : ipc.shm_open("test_seg", 8192)
assert(tok >= 1)

tok2 : ipc.shm_open("other", 4096)
assert(tok2 >= 1)
assert(tok2 != tok)

ipc.shm_close(tok)
ipc.shm_close(tok2)
print("ipc shm ok")
