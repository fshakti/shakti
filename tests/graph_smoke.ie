# graph smoke — import graph.ie wrapper, one triple round-trip
import graph

graph.create()
graph.add("alpha", "rel", "beta")
assert(graph.count() = 1)

row : graph.query("alpha", "rel", "beta")
assert(shape(row)[0] = 1)
assert(row["object"][0] = "beta")

print("graph smoke ok")
