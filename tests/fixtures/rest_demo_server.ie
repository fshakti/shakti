# Demo server — /ping -> pong, /echo -> echo body
import rest

port : 19091
srv : rest.listen(port)
c : rest.accept(srv)
req : rest.read(c)
body : "pong"
if req["path"] = "/echo":
    body : req["body"]
rest.write(c, 200, body, "text/plain")
rest.close(c)
rest.close(srv)
